/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateRangeValidationDirective } from './date-range-validation.directive';

describe('Directive: DateRangeValidation', () => {
  it('should create an instance', () => {
    const directive = new DateRangeValidationDirective();
    expect(directive).toBeTruthy();
  });
});
